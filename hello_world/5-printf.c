#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char MyString[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s", MyString);
	return (0);
}
